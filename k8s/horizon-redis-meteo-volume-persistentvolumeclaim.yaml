apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: horizon-redis-meteo-volume
  name: horizon-redis-meteo-volume
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
