apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: analytics-reports-claim0
  name: analytics-reports-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
