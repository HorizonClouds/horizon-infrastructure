apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: users-claim0
  name: users-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
