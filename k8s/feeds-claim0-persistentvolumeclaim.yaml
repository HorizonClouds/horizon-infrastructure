apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: feeds-claim0
  name: feeds-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
