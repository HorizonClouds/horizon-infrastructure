apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: publications-claim0
  name: publications-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
