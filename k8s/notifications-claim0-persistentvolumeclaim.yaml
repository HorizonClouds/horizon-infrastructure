apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: notifications-claim0
  name: notifications-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
