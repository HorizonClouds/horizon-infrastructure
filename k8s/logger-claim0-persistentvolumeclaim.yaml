apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: logger-claim0
  name: logger-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
